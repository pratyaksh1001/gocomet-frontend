/** @type {import('next').NextConfig} */
const nextConfig = {
    allowedDevOrigins: [
        "127.0.0.1",
        "http://127.0.0.1:8000",
        "http://localhost:8000",
    ],
};

export default nextConfig;
